<%= form_for @post, url: posts_path, html: {multipart: true} do |f| %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit 'Post Image' %>
<% end %>
